library(dfgraph)

# TODO:
# - The context is probably parse_nodes()

test_that("Normal expressions are parsed into 1 node row each", {
    
})

test_that("An if/else expression is parsed into multiple node rows", {
    
})

test_that("If/else symmetric nodes are correctly identified", {
    
})

test_that("If/else asymmetric nodes are correctly identified", {
    
})